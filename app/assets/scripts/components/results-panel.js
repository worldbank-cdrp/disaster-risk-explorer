import React from 'react'

import { } from '../actions'

const Results = React.createClass({
  propTypes: {
  },
  render: function () {
    return (
      <section id='Results'>
      </section>
    )
  }
})

export default Results
