import React from 'react'

import { } from '../actions'

const Header = React.createClass({
  propTypes: {
  },
  render: function () {
    return (
      <div id='Header'>
      </div>
    )
  }
})

export default Header
