import React from 'react'

import { } from '../actions'

const Legend = React.createClass({
  propTypes: {
  },
  render: function () {
    return (
      <div id='Legend'>
      </div>
    )
  }
})

export default Legend
